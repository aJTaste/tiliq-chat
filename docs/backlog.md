# バックログ（次にやること・検討中のアイデア）

**ここには「今open な項目」だけを置く。** 完了した項目はここから削除する（経緯・実装詳細は`docs/phases/`側に残っているため、ここに取り消し線付きで残す必要はない）。Phase区切りごとに `/wrap-phase` で整理する。

Phase 15（2026-08-12）の時点で「一旦完成・既知の問題なし」の区切りに到達済み。以降は新機能・仕様変更のラウンドとして、ユーザーから「許可不要で計画→実装を繰り返してよい、コミットも自由に行ってよい」との承認済み（2026-08-12）。**ただしデプロイ（Vercel本番公開）だけは明示的な指示があるまで着手しない**（ユーザーより「私が指示しない限り行いません」と明言済み。数ヶ月後の見込み）。

## 着手候補（優先度は状況次第で判断してよい）

- **グループチャットM5以降：** `messages_insert_member_not_blocked`の「メンバーの誰か1人をブロックしただけで全体送信が止まる」という過剰に強い挙動の見直し（意図的に残置中、見直すかは要判断）。グループ名変更・アバター設定はPhase 24で実装済み
- **サイドバー再設計の残タスク（Phase 23由来）：** サイドバー幅`md:w-80`の実機確認後の再調整（`GroupMembersPanel.tsx`のModal追従・`docs/srs.md` 3.2.1節の整合は対応済み）
- **チャット切り替え時のさらなる体感速度改善：** ヘッダー・入力欄をSuspenseで据え置き、メッセージ一覧だけを遅延ストリーミングする案。`app/(shell)/chat/[roomId]/template.tsx`によるroomId切り替え時の強制リマウント設計（AuthGateのsessionStorage再チェック担保のため導入）との整合性を先に設計する必要がある
- **自動テスト基盤の導入：** Phase 15で見送り・手動QA運用継続の方針を確定済み（`docs/srs.md` 3.9節にも明記）。この決定自体は蒸し返し不要だが、グループチャットUI実装で既存機能への影響範囲が広がったため、要否を再検討する候補として残す

## 実機フィードバックで見つかった小粒課題（優先度未確定・ユーザーと相談のうえ選ぶ）

- 一時チャットの名前を作成後にリネームしたい（Phase 28で作成時の名前付け自体は対応済み。作成後のリネームは未対応のまま）
- サイドバーの会話一覧に既読状態を反映したい（Phase 29でチャット画面内の既読表示は対応済み。一覧側は未対応のまま。`get_conversation_list`/`get_group_conversation_list`の戻り値拡張が必要）
- DM既読の個人単位オプトアウト（自分の既読を相手に送信しない設定）。Phase 29でユーザー判断によりv1スコープ外に決定。要望が出れば`user_settings`への新規カラム追加で対応

## デザイン方針（継続的なガイドライン。単発タスクではない）

- 種別を切り替えるUIは基本的にタブ型を使う（状況に応じて使い分け可）。Phase 23のサイドバー「検索/一覧」タブ・サブフィルタで既に踏襲済み。今後の画面設計でも踏襲する

## 将来検討（今すぐ着手しない・方向性確認が先）

- **複数チャットレイアウト対応：** LINE風（現状）以外にDiscord風（全員左揃え、非吹き出し）などから選べるようにしたいという提案。DB設計への影響はほぼなし（`MessageBubble.tsx`等の表示層差し替えのみ）。グループチャットとの相性（「誰が発言したか」表示はグループでこそ価値が出る）を踏まえ、着手するとしてもグループチャットが一定成熟してからが自然。**現時点では実装しないこと**（ユーザー確認済み）。設定保存先は`user_settings`に新カラム（例：`chat_layout`）を追加する想定
- **デプロイ（Vercel本番）：** 上記の通りユーザーからの明示的な指示待ち

## 運用メモ

- 2アカウント運用（メイン/サブ）：両アカウントは同じローカルリポジトリを操作するため、アカウント切り替え時は必ず`git pull`してから作業し、作業単位ごとに`commit`・`push`する。同時に両アカウントで並行編集しない
- **Claude Code利用の中断（2026-08-17〜）：** メインアカウントのPro契約が2026-08-17で一旦終了し、再開時期は未定（数週間〜数ヶ月の見込みだが確定していない）。次回セッション再開時は、まずPhase 26〜29（プロフィール編集機能・スクロールバー見た目調整・一時チャットの名前付け・既読機能）の「動作確認してほしい項目」（`docs/phases/phase-26-profile-editing.md` 〜 `docs/phases/phase-29-read-receipts.md`）の実機確認から着手するのが自然。Phase 28・29はDBマイグレーションを含む（それぞれ`create_temp_dm_room`/`get_conversation_list`の変更、`room_members.last_read_at`＋`mark_room_read` RPC新設）ため、特に優先して確認したい
